<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
 <mapper namespace="com.biz.iolist.persistence.ProductDAO">
 	
 	<select id="selectAll" resultType="ProVO">
 		SELECT * FROM tbl_iolist
 	</select>
 	
 	<select id="findById" resultType="ProVO">
 		SELECT * FROM tbl_iolist WHERE io_pcode = #{io_pcode}
 	</select>
 	
 	<insert id="insert">
 		INSERT INTO tbl_iolist (	
		 	io_pcode,
			io_pname,
			io_date,
			io_time,
			io_input,
			io_iprice,
			io_oprice,
			io_quan,
			io_total	
	 	
		) VALUES (
	
			#{io_pcode},
			#{io_pname},
			#{io_date},
			#{io_time},
			#{io_input},
			#{io_iprice},
			#{io_oprice},
			#{io_quan},
			#{io_total}
	)
 	</insert>
 	
 	<delete id="delete"></delete>
 
 </mapper> 